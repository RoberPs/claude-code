node_modules/

# Base de datos local: se genera con `npm run db:init`.
# No se versiona porque contiene hashes de contraseñas reales.
kohi.db
kohi.db-shm
kohi.db-wal

# Artefactos de las sesiones de Playwright MCP
.playwright-mcp/
*.png

.env
.claude/settings.local.json
